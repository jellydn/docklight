# Implementation Plans

Generated by the improve skill on 2026-06-15. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                         | Priority | Effort | Depends on                 | Status |
| ---- | --------------------------------------------- | -------- | ------ | -------------------------- | ------ |
| 004  | Clean up SSH bridge documentation consistency | P2       | S      | completed SSH bridge plans | DONE   |
| 005  | Add opt-in auto-update for installed VPSes    | P2       | M      | -                          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale: finding fixed independently or approach abandoned)

## Dependency notes

- Plans 001-003 were completed and removed from this active plan set in commit `6af8342`; plan 004 remains as a docs consistency follow-up.
- Plan 005 is independent of plan 004. It builds on the existing installer deployment flow at `scripts/install.sh`, which already uses `REPO_URL`, `BRANCH`, and `dokku git:sync --build`.

## Verification baseline

Use these commands for installer/docs work:

- `bash -n scripts/install.sh`
- `just server-typecheck`
- `just server-lint`

Run `just server-test` if a plan changes server TypeScript.

## Related context

- `scripts/install.sh` is the supported one-line installer for fresh and rerun VPS setup.
- Auto-update must be opt-in because unattended branch tracking changes the operator's deployment trust model.
